// frontend/src/fintrack/pages/overview/domains/DebtDomain.tsx
//
// Debt on level 2. No single series: the two legs are drawn apart, and they and
// the breakdown by counterparty are later commits.

import { DomainCompositionProps } from './domainScreen';

function DebtDomain(_props: DomainCompositionProps<'debt'>) {
 return null;
}

export default DebtDomain;
