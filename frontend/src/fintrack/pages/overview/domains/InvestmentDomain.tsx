// frontend/src/fintrack/pages/overview/domains/InvestmentDomain.tsx
//
// Investment on level 2. No series: an accumulation only rises. The
// reconciliation, balance per account and contribution history are later commits.

import { DomainCompositionProps } from './domainScreen';

function InvestmentDomain(_props: DomainCompositionProps<'investment'>) {
 return null;
}

export default InvestmentDomain;
